{
  "name": "noticiel-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Serveur MCP (Cloudflare Workers) exposant le catalogue de notices Noticiel aux agents IA.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "wrangler dev"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.0",
    "agents": "^0.0.95",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.20.0"
  }
}
